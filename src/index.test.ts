import { RelayEnumGenerator } from './index';

describe('RelayEnumGenerator', () => {
  it('should return relay-enum-generator', () => {
    expect(RelayEnumGenerator()).toBe('relay-enum-generator');
  });
});
